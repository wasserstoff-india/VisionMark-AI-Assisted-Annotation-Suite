export declare type GlobalyzerRet = {
    base: string;
    glob: string;
    isGlob: boolean;
};
export declare type GlobalyzerOpt = {
    strict?: boolean;
};
/**
 * Parse a glob path, and split it by static/glob part
 * @param {String} pattern String path
 * @param {Object} [opts] Options
 * @param {Object} [opts.strict=false] Use strict parsing
 * @returns {Object} object with parsed path
 */
export declare function globalyzer(pattern: string, opts?: GlobalyzerOpt): GlobalyzerRet;
export default globalyzer;
