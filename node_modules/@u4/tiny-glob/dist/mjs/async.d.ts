import { BlobOptions } from './common.js';
export declare let CACHE: {
    [key: string]: boolean;
};
/**
 * Find files using bash-like globbing.
 * All paths are normalized compared to node-glob.
 * @param str Glob string
 * @returns array containing matching files
 */
export declare function glob(str: string, opts?: BlobOptions): Promise<string[]>;
export default glob;
